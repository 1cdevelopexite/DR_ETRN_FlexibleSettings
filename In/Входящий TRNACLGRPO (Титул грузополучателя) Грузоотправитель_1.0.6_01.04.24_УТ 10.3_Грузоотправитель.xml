<ValueTree xmlns="http://v8.1c.ru/8.1/data/core" xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:type="ValueTree">
	<column>
		<Name xsi:type="xs:string">Использовать</Name>
		<ValueType/>
	</column>
	<column>
		<Name xsi:type="xs:string">ТипТранзакции</Name>
		<ValueType/>
	</column>
	<column>
		<Name xsi:type="xs:string">Кнопка</Name>
		<ValueType/>
	</column>
	<column>
		<Name xsi:type="xs:string">СлужебныеТекст</Name>
		<ValueType/>
	</column>
	<column>
		<Name xsi:type="xs:string">Служебные</Name>
		<ValueType/>
	</column>
	<column>
		<Name xsi:type="xs:string">КоллекцияТЧ</Name>
		<ValueType/>
	</column>
	<row>
		<Value xsi:type="xs:boolean">true</Value>
		<Value xsi:type="xs:string">Входящий TRNACLGRPO (Титул грузополучателя) Грузоотправитель</Value>
		<Value xsi:type="xs:string">Т3 Титул грузополучателя</Value>
		<Value xsi:type="xs:string"/>
		<Value xsi:type="xs:string">//+     
Грузоперевозчик = ВернутьЗначениеДопРеквизитаИзДокумента("Грузоперевозчик", Приемник.ДополнительныеРеквизиты);

НоваяЗапись = Приемник.ДополнительныеРеквизиты.Добавить();
НоваяЗапись.Реквизит = "Организация";
Если ТипЗнч(Грузоперевозчик) = Тип("СправочникСсылка.Организации") Тогда 
	НоваяЗапись.Значение = Грузоперевозчик;
Иначе
	НоваяЗапись.Значение = ВернутьЗначениеДопРеквизитаИзДокумента("Грузоотправитель", Приемник.ДополнительныеРеквизиты);
КонецЕсли;

НоваяЗапись = Приемник.ДополнительныеРеквизиты.Добавить();
НоваяЗапись.Реквизит = "Заказчик";
НоваяЗапись.Значение = ВернутьЗначениеДопРеквизитаИзДокумента("Грузополучатель", Приемник.ДополнительныеРеквизиты);
//-

//+Адреса 
АдресМестаВыгрузки = "";
АдресМестаВыгрузкиСтруктура = Новый Структура;

Для Каждого Строка Из Приемник.ДополнительныеРеквизиты Цикл
	Если Не Найти(Строка.Реквизит, "АдресМестаВыгрузки") = 0 Тогда
		АдресМестаВыгрузкиСтруктура.Вставить(СтрЗаменить(Строка.Реквизит, "АдресМестаВыгрузки", ""), Строка.Значение);
		АдресМестаВыгрузки = АдресМестаВыгрузки + Строка.Значение + ", ";
		
		Если Строка.Реквизит = "АдресМестаВыгрузкиИндекс" Тогда	
			АдресМестаВыгрузкиСтруктура.Вставить("ТипАдреса", "АдрРФ");
		ИначеЕсли Строка.Реквизит = "АдресМестаВыгрузкиКодСтр" Тогда
			АдресМестаВыгрузкиСтруктура.Вставить("ТипАдреса", "АдрСтр");
		ИначеЕсли Строка.Реквизит = "АдресМестаВыгрузкиКодГАР" Тогда
			АдресМестаВыгрузкиСтруктура.Вставить("ТипАдреса", "КодГАР");
		КонецЕсли;
	КонецЕсли;
КонецЦикла;

НоваяЗапись = Приемник.ДополнительныеРеквизиты.Добавить();
НоваяЗапись.Реквизит = "АдресМестаВыгрузки";
НоваяЗапись.Значение = Лев(АдресМестаВыгрузки, СтрДлина(АдресМестаВыгрузки)-2);
//-Адреса

Если ЗначениеЗаполнено(СтруктураДокумента) Тогда
	ТЗШтрафы = Новый ТаблицаЗначений;
	ТЗАкты = Новый ТаблицаЗначений;
	ТЗСвАктВзв = Новый ТаблицаЗначений;
    ТЗГруз = Новый ТаблицаЗначений; 
    СтруктураДанных = Новый Структура;

	ДобавитьКолонкиВТаблицуТЧ(ТЗГруз, ТЗШтрафы, ТЗАкты, ТЗСвАктВзв, Приемник.ВидДокумента); 
	
	ТЗГруз.Колонки.Добавить("Номенклатура");
	ТЗГруз.Колонки.Добавить("Маркировка");
        
	//Проверяем наличие ТЧ
	Если СтруктураДокумента.Свойство("Документ_СодИнфГП_ПриемГрузГП_СвПринПоНаим_") Тогда 
		
		МассивДанных = СтруктураДокумента.Документ_СодИнфГП_ПриемГрузГП_СвПринПоНаим_;							
			
		//Пишем данные в таблицу значений
		Для Каждого СтруктураТЧ Из МассивДанных Цикл 
			Для Каждого ЭлСтруктураТЧ Из СтруктураТЧ Цикл 
				Для Каждого МасСтруктур Из ЭлСтруктураТЧ.Значение Цикл
					НоваяСтрока = ТЗГруз.Добавить(); 
					ЗаполнитьЗначенияСвойств(НоваяСтрока, МасСтруктур);	
				КонецЦикла;
			КонецЦикла;		
		КонецЦикла; 
		
	Иначе 
		
		НоваяСтрока = ТЗГруз.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, СтруктураДокумента);   
				
	КонецЕсли; 
	
	//Переименовываем колонки, чтобы их видела форма
	Для Каждого Колонка Из ТЗГруз.Колонки Цикл
		МассивЗначений = DR_СтрРазделить(Колонка.Имя,"_");						
		Колонка.Имя = МассивЗначений[МассивЗначений.Количество()-1];
		
		Если Колонка.Имя = "КолМест" Тогда
			Колонка.Имя = "КолМестГр";
		КонецЕсли;	
	КонецЦикла; 
		
	ЗаписатьТаблицуЗначенийВСтруктуру(ТЗГруз, "СведенияОГрузе", СтруктураДанных);             

	ТЗШтрафы.Колонки.Добавить("ОтмАктВзв");	
	Если СтруктураДокумента.Свойство("Документ_СодИнфГП_ОтметГП_РазмШтр_") Тогда 
		
		МассивДанных = СтруктураДокумента.Документ_СодИнфГП_ОтметГП_РазмШтр_;						
		
		Для Каждого СтруктураТЧ Из МассивДанных Цикл
			НоваяСтрока = ТЗШтрафы.Добавить(); 
			ЗаполнитьЗначенияСвойств(НоваяСтрока, СтруктураТЧ);			

		КонецЦикла; 
		
		Для Каждого Колонка Из ТЗШтрафы.Колонки Цикл
			МассивЗначений = DR_СтрРазделить(Колонка.Имя,"_");						
			Колонка.Имя = МассивЗначений[МассивЗначений.Количество()-1]; 	
		КонецЦикла;
           
	Иначе 
		
		НоваяСтрока = ТЗШтрафы.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, СтруктураДокумента);   
				
	КонецЕсли; 
	
	//Переименовываем колонки, чтобы их видела форма
	Для Каждого Колонка Из ТЗШтрафы.Колонки Цикл
		
		МассивЗначений = DR_СтрРазделить(Колонка.Имя,"_");						
		Колонка.Имя = МассивЗначений[МассивЗначений.Количество()-1];
			
	КонецЦикла; 
		
	ЗаписатьТаблицуЗначенийВСтруктуру(ТЗШтрафы, "СведенияШтрафов", СтруктураДанных);
	
    ТЗАкты.Колонки.Добавить("ОтмАктВзв");
    Если СтруктураДокумента.Свойство("Документ_СодИнфГП_ОтметГП_СвАкт_") Тогда 
		
		МассивДанных = СтруктураДокумента.Документ_СодИнфГП_ОтметГП_СвАкт_;								
		
		Для Каждого СтруктураТЧ Из МассивДанных Цикл
			НоваяСтрока = ТЗАкты.Добавить(); 
			ЗаполнитьЗначенияСвойств(НоваяСтрока, СтруктураТЧ);			

		КонецЦикла; 
		
		Для Каждого Колонка Из ТЗАкты.Колонки Цикл
			МассивЗначений = DR_СтрРазделить(Колонка.Имя,"_");						
			Колонка.Имя = МассивЗначений[МассивЗначений.Количество()-1]; 	
		КонецЦикла;  	
				
	Иначе 
		
		НоваяСтрока = ТЗАкты.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, СтруктураДокумента);   
				
	КонецЕсли; 
	
	//Переименовываем колонки, чтобы их видела форма
	Для Каждого Колонка Из ТЗАкты.Колонки Цикл
		
		МассивЗначений = DR_СтрРазделить(Колонка.Имя,"_");						
		Колонка.Имя = МассивЗначений[МассивЗначений.Количество()-1];
			
	КонецЦикла; 
		
	ЗаписатьТаблицуЗначенийВСтруктуру(ТЗАкты, "СведенияИныхАктов", СтруктураДанных);
	
	ТЗСвАктВзв.Колонки.Добавить("ОтмАктВзв");	
	Если СтруктураДокумента.Свойство("Документ_СодИнфГП_ОтметГП_СвАктВзв_") Тогда 
		
		МассивДанных = СтруктураДокумента.Документ_СодИнфГП_ОтметГП_СвАктВзв_;						
		
		Для Каждого СтруктураТЧ Из МассивДанных Цикл
			НоваяСтрока = ТЗСвАктВзв.Добавить(); 
			ЗаполнитьЗначенияСвойств(НоваяСтрока, СтруктураТЧ);			

		КонецЦикла; 
		
		Для Каждого Колонка Из ТЗСвАктВзв.Колонки Цикл
			МассивЗначений = DR_СтрРазделить(Колонка.Имя,"_");						
			Колонка.Имя = МассивЗначений[МассивЗначений.Количество()-1]; 	
		КонецЦикла;
  	
	Иначе 
		
		НоваяСтрока = ТЗСвАктВзв.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, СтруктураДокумента);   
				
	КонецЕсли; 
	
	//Переименовываем колонки, чтобы их видела форма
	Для Каждого Колонка Из ТЗСвАктВзв.Колонки Цикл
		
		МассивЗначений = DR_СтрРазделить(Колонка.Имя,"_");						
		Колонка.Имя = МассивЗначений[МассивЗначений.Количество()-1];
			
	КонецЦикла; 
		
	ЗаписатьТаблицуЗначенийВСтруктуру(ТЗСвАктВзв, "СведенияАктовВзвешивания", СтруктураДанных);

	СписокДляДобавления = Новый Соответствие;
	СписокДляДобавления.Вставить("Таблицы"					, СтруктураДанных);
	СписокДляДобавления.Вставить("АдресМестаВыгрузкиДанные"	, АдресМестаВыгрузкиСтруктура);			
			
	Для Каждого Элемент Из СписокДляДобавления Цикл

		НоваяСтрока = Приемник.ДополнительныеРеквизиты.Добавить();	    
	    НоваяСтрока.Реквизит = Элемент.Ключ;
		НоваяСтрока.ХранилищеЗначения = Новый ХранилищеЗначения(Элемент.Значение);
		
	КонецЦикла;
		
КонецЕсли;      

Приемник.Статус = ПолучитьЗначение_Перечисления("Статусы","Обработан");             
</Value>
		<Value xsi:type="ValueListType">
			<valueType/>
			<lastId xsi:type="xs:decimal">10</lastId>
			<item>
				<value xsi:type="xs:string">ДополнительныйПолучатель</value>
				<id xsi:type="xs:decimal">5</id>
			</item>
			<item>
				<value xsi:type="xs:string">Грузоперевозчик</value>
				<id xsi:type="xs:decimal">6</id>
			</item>
			<item>
				<value xsi:type="xs:string">Грузополучатель</value>
				<id xsi:type="xs:decimal">7</id>
			</item>
			<item>
				<value xsi:type="xs:string">Грузоотправитель</value>
				<checkState>1</checkState>
				<id xsi:type="xs:decimal">8</id>
			</item>
			<item>
				<value xsi:type="xs:string">Синоним</value>
				<presentation>Т3 Титул грузополучателя</presentation>
				<checkState>1</checkState>
				<id xsi:type="xs:decimal">9</id>
			</item>
			<item>
				<value xsi:type="xs:string">ИмяКнопки</value>
				<presentation>ON_TRNACLGRPO_GO_In</presentation>
				<checkState>1</checkState>
				<id xsi:type="xs:decimal">10</id>
			</item>
		</Value>
	</row>
</ValueTree>